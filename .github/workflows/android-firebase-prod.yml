# Reusable: build convos-client prodRelease APK and upload to Firebase App
# Distribution. The 3x/day dev-stream prod build (prod signing, tester group
# fixed in the lane). Runs on the CALLER's checkout. Prod signing comes from
# the caller's `prod-signing` environment, not repo-level secrets; only
# FIREBASE_SERVICE_ACCOUNT_JSON + CACHIX_AUTH_TOKEN arrive via `secrets: inherit`.
name: Android Firebase Prod (reusable)

on:
  workflow_call:
    inputs:
      runner:
        description: Runner label for the build job
        type: string
        default: blacksmith-8vcpu-ubuntu-2404
      project-dir:
        description: Directory containing the gradle root (and fastlane/ stub)
        type: string
        default: android
      firebase-app-id:
        description: "Firebase Android app id (1:NNN:android:HEX)"
        type: string
        required: true
    secrets:
      FIREBASE_SERVICE_ACCOUNT_JSON:
        description: Base64-encoded service account JSON
        required: true
      CACHIX_AUTH_TOKEN:
        required: true

jobs:
  build:
    name: Build & upload to Firebase App Distribution
    runs-on: ${{ inputs.runner }}
    # Serialize prod-signing usage at the job itself. Queue, never cancel.
    concurrency:
      group: firebase-prod-${{ github.repository }}
      cancel-in-progress: false
    # Resolves the CALLER's prod-signing environment: its secrets and
    # deployment-branch policy (dev only) apply here.
    environment: prod-signing
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true

      - name: Set up Nix
        # Qualified ref: reusable workflows check out the CALLER's repo.
        # sticky-disk gates on the label so non-Blacksmith callers are
        # unaffected.
        uses: xmtplabs/convos-releases/.github/actions/setup-nix@main
        with:
          sticky-disk: ${{ startsWith(inputs.runner, 'blacksmith') }}
          cachix-auth-token: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Mount gradle cache sticky disk
        if: startsWith(inputs.runner, 'blacksmith')
        uses: useblacksmith/stickydisk@v1
        with:
          key: ${{ github.repository }}-gradle
          path: /home/runner/.gradle

      - name: Own gradle cache
        if: startsWith(inputs.runner, 'blacksmith')
        run: sudo chown "$(id -u):$(id -g)" /home/runner/.gradle

      - name: Enter dev shell
        uses: nicknovitski/nix-develop@v1
        with:
          arguments: "--accept-flake-config"

      - name: Decode prod keystore + Firebase service account
        env:
          PROD_KEYSTORE_B64: ${{ secrets.PROD_KEYSTORE_BASE64 }}
          PROD_KEYSTORE_PASSWORD: ${{ secrets.PROD_KEYSTORE_PASSWORD }}
          PROD_KEY_ALIAS: ${{ secrets.PROD_KEY_ALIAS }}
          PROD_KEY_PASSWORD: ${{ secrets.PROD_KEY_PASSWORD }}
          FIREBASE_SA_BASE64: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
        # Secret values never reach the log (stdout redirected, no command
        # echo, Actions masking); printf avoids echo's escape processing.
        # Guard EVERY secret: a missing one fails minutes later with a cryptic
        # gradle error, and an empty PROD_KEY_ALIAS silently overrides gradle's default.
        run: |
          [ -n "$PROD_KEYSTORE_B64" ] || { echo "::error::PROD_KEYSTORE_BASE64 secret is missing (check the prod-signing environment)"; exit 1; }
          [ -n "$PROD_KEYSTORE_PASSWORD" ] || { echo "::error::PROD_KEYSTORE_PASSWORD secret is missing (check the prod-signing environment)"; exit 1; }
          [ -n "$PROD_KEY_ALIAS" ] || { echo "::error::PROD_KEY_ALIAS secret is missing (check the prod-signing environment)"; exit 1; }
          [ -n "$PROD_KEY_PASSWORD" ] || { echo "::error::PROD_KEY_PASSWORD secret is missing (check the prod-signing environment)"; exit 1; }
          [ -n "$FIREBASE_SA_BASE64" ] || { echo "::error::FIREBASE_SERVICE_ACCOUNT_JSON secret is missing"; exit 1; }
          printf '%s' "$PROD_KEYSTORE_B64" | base64 -d > "$RUNNER_TEMP/prod-upload.jks"
          printf '%s' "$FIREBASE_SA_BASE64" | base64 -d > "$RUNNER_TEMP/firebase-sa.json"

      - name: Run fastlane android prod_adhoc
        working-directory: ${{ inputs.project-dir }}
        env:
          # Absolute path: gradle's file() resolves relative paths against
          # the app module dir, not the gradle root.
          PROD_KEYSTORE_PATH: ${{ runner.temp }}/prod-upload.jks
          PROD_KEYSTORE_PASSWORD: ${{ secrets.PROD_KEYSTORE_PASSWORD }}
          PROD_KEY_ALIAS: ${{ secrets.PROD_KEY_ALIAS }}
          PROD_KEY_PASSWORD: ${{ secrets.PROD_KEY_PASSWORD }}
          FIREBASE_APP_ID_ANDROID_PROD: ${{ inputs.firebase-app-id }}
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ runner.temp }}/firebase-sa.json
        run: fastlane android prod_adhoc

      - name: Clean up decoded secrets
        if: always()
        run: rm -f "$RUNNER_TEMP/prod-upload.jks" "$RUNNER_TEMP/firebase-sa.json"

      - name: Upload APK artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: convos-android-firebase-prod-${{ github.run_id }}
          path: ${{ inputs.project-dir }}/app/build/outputs/apk/prod/release/*.apk
          if-no-files-found: warn
          retention-days: 14

      - name: Release sticky-disk mounts for commit
        # Gradle/Kotlin build daemons outlive the build with /nix/store
        # jars (and ~/.gradle locks) held open, so the sticky-disk
        # post-steps' umounts get EBUSY and the disks never commit —
        # every run then starts cold. Kill remaining users of the mounts
        # EXCEPT the cachix daemon: its own post step stops it cleanly
        # (and fails the job if it is already dead) before the nix
        # sticky-disk post unmounts.
        # mountpoint guard: fuser -m on a non-mountpoint would target the
        # whole containing filesystem (/) and kill everything.
        # /usr/bin/bash, not the dev-shell bash: the latter lives in
        # /nix and would be killed mid-step.
        if: always() && startsWith(inputs.runner, 'blacksmith')
        shell: /usr/bin/bash {0}
        run: |
          targets=()
          for m in /nix /home/runner/.gradle; do
            mountpoint -q "$m" && targets+=("$m")
          done
          [ "${#targets[@]}" -gt 0 ] || exit 0
          keep="$(pgrep -f 'cachix daemon' || true)"
          for pid in $(sudo fuser -m "${targets[@]}" 2>/dev/null); do
            echo "$keep" | grep -qx "$pid" || sudo kill -9 "$pid" 2>/dev/null
          done
          sleep 2
          exit 0
